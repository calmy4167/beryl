# Calmy 核心用户流程与界面规格

> Today、Capture、Matter 三条主路径。  
> 版本：v0.1 · 日期：2026-08-19 · 关联：[MVP 产品范围](Calmy_MVP产品范围与不做清单_2026-08-19.md)、[领域模型](Calmy_领域模型与状态机_2026-08-19.md)

## 1. 交互原则

> 用户先处理现实，再整理结构；界面负责减少选择压力，而不是把全部领域模型暴露给用户。

- 一级入口少，上下文聚合多。
- 每个页面最多突出一个主行动。
- Capture 先保存，分类可以晚一点。
- 复杂才展开；简单动作不进入完整流程。
- 用状态和事实帮助判断，不用颜色和完成率审判用户。
- 允许跳过、暂停、降载、改写、删除草稿和恢复历史。

## 2. MVP 信息架构

| 入口 | 用户问题 | 首版重点 |
|---|---|---|
| `Today` | 我现在应该做什么？为什么？什么可以不做？ | 方向、承载、核心行动、放下事项 |
| `Matters` | 我正在面对哪些现实事项？ | 当前 Matter、Cycle、主要矛盾、最近反馈 |
| `People` | 这些事情与谁有关？ | MVP 可先做自己的 Context |
| `Library` | 我长期留下了什么？ | Insight、Seed、资源和导出入口 |
| `Search` | 过去发生过什么？ | 全文搜索 + 类型 / Matter / 日期筛选 |
| `Capture` | 我现在想到 / 发生了什么？ | 全局快捷入口，默认进入 Inbox |

桌面端：左侧主导航 + 主内容区。  
移动端：底部导航 + 固定 Capture。  
离线状态：顶部轻提示，不阻断核心记录。

## 3. Today：每日定向而非任务墙

### 3.1 页面层级

1. **方向**：当前最重要的长期 Matter 和 Cycle Theme。
2. **当下**：身体、心理、精力和今日承载量。
3. **行动**：1–3 个核心 Action，分为“必须守住”和“有余力再做”。
4. **减法**：今天什么可以不做。
5. **记录入口**：随时 Capture，不要求离开 Today 去分类。

### 3.2 Morning Opening 三问

| 问题 | 输入形式 | 系统行为 |
|---|---|---|
| 今天真正重要的是什么？ | 选择 / 新建 1–3 个 Action | 连接到 Matter / Cycle；超过 3 个时温和提醒 |
| 为什么值得做？ | 自动显示连接路径，可补一句理由 | 让行动连接长期方向，不要求长篇目标 |
| 今天什么可以不做？ | 选择候选事项或自由输入 | 标记 `let_go / later`，不计入失败 |

第三问是关键：减法必须是产品行为，不是隐藏在说明文字里的哲学。

### 3.3 承载策略

| 状态 | 默认呈现 | 默认限制 |
|---|---|---|
| 很好 | 核心行动 + 1 个挑战性候选 | 不自动增加任务 |
| 普通 | 只显示核心行动 | 控制总量 |
| 疲惫 | 必须守住 + 一个最小行动 | 其余折叠到“有余力再做” |
| 很差 | 恢复、吃饭、睡眠和必要责任 | 建议暂停非必要 Matter |

## 4. Capture：低摩擦入口

### 4.1 主流程

1. 用户输入一句话或一段文字。
2. 按 Enter 即保存为 Inbox Record。
3. 保存后只给出轻量提示“已记录”，不阻塞继续输入。
4. 用户空闲时进入 Triage，看到系统建议的类型、Matter、Person、Action 和标签。
5. 用户可以接受、修改、跳过；跳过不会丢失原文。
6. 确认后生成 Relation 或 Action；原始 Capture 仍作为来源 Record 保留。

### 4.2 Triage 建议

```text
原文：今天又刷了很久视频，Java 没有开始，主要是昨晚睡得太晚。

建议：
- 类型：Record
- 关联 Matter：学习 Java
- 可能条件：睡眠不足
- 可能的明日调整：只保留 45 分钟 Java
- 需要确认：是
```

Capture 成功的标准不是分类准确率最高，而是用户愿意记录；分类可以晚一点，现实不应因为分类成本而丢失。

### 4.3 何时建议升级为 Matter

系统不应把每条 Capture 都升级成 Matter。满足以下至少两项时，才显示“创建 Matter”建议：

- 会持续一段时间；
- 包含多个行动；
- 需要判断、取舍或资源安排；
- 会消耗时间、精力、金钱或关系资源；
- 值得复盘并形成经验。

否则优先保留为 Record 或 Action。用户也可以永远拒绝升级。

## 5. Matter 详情页

| 区域 | 内容 | 交互 |
|---|---|---|
| 顶部摘要 | 名称、状态、trajectory、当前 Cycle、主要矛盾 | 暂停 / 恢复 / 归档；编辑关键字段 |
| 当前 Cycle | Theme、当前 Stage、最近行动、最近反馈 | 切换 Cycle；查看上下文 |
| 今日连接 | 与 Today 相连的 Action | 加入 / 移出 Today，不复制 Action |
| 历史 | Record、复盘、负向变化、资产损耗 | 按时间和类型筛选 |
| 下一轮 | Seed、未解决问题、建议方向 | 用户确认后才创建新 Cycle |

### 5.1 Matter 首次创建

只要求最小字段：

- 这件事叫什么？
- 为什么现在值得处理？
- 如果只做一件最小行动，是什么？

“主要矛盾”可以在第一次复盘后补，不要求用户一开始就说得很完整。

### 5.2 五行显示规则

- 默认只显示自然语言解释，例如“当前处于实践阶段”；五行名称放在详情或设置中。
- 用户主动打开“过程模型”后，才显示 Wood / Fire / Earth / Metal / Water。
- Stage 可以暂停、回退和跳转；UI 不用错误颜色阻止用户。
- 子 Cycle 默认隐藏，只显示“有一个子事项”和展开入口。
- 没有足够复杂度时，不显示展开子 Cycle 的诱导入口。
- 简单 Action 不创建 Stage，也不要求用户完成一套流程。

## 6. Action 执行与记录

1. 用户从 Today 或 Matter 打开 Action，看到它连接的 Matter、Cycle 和原因。
2. 用户可以开始、完成、跳过、取消或改写；不要求填写长表单。
3. 完成只记录事实，不自动评价“成功”。
4. 用户可立即追加 Record：做了什么、花了多久、遇到什么、状态如何。
5. 如果 Action 被跳过，系统只询问：延期、删除、暂停还是记录原因。

### 6.1 Action 卡片最小信息

```text
完成 JwtService 的最小实现并跑通测试
学习 Java · Cycle 05 · Fire
为什么：解决当前“能读代码但不能独立完成模块”的矛盾
[开始] [完成] [跳过] [记录现实]
```

## 7. Evening Review：观 → 察 → 调

| 步骤 | 页面提示 | 产出 |
|---|---|---|
| 观 | 今天实际发生了什么？时间流向哪里？ | 事实 Record |
| 察 | 哪些条件影响了行动？主动娱乐还是无意识消耗？ | 条件、模式、反馈 |
| 调 | 明天继续、减量、暂停、换方法还是放弃？ | Today 调整、状态变更 |
| 土库 | 今天留下了什么代码、经验、问题、损耗或 Seed？ | Insight / Seed / 资产变化 |

### 7.1 不强迫完成复盘

复盘可以只写一句话，也可以跳过。系统应该保留“没有复盘”这一事实，但不能把它自动解释为懒惰或失败。

## 8. 空状态、错误与可访问性

- 无 Matter：显示“先记录一个现实事项”，提供文本框，不先展示复杂术语。
- 无 Action：允许只做 Record 或休息，不强迫添加行动。
- 离线：顶部显示离线状态，但 Capture、Today、Record 仍可用。
- 同步冲突：显示差异与选项，不隐藏、不自动覆盖。
- 键盘：Capture、搜索、完成 Action、打开 Command Palette 都有快捷键。
- 移动端：主行动触控区域至少 44px；颜色不单独承担状态含义。
- 长文本：允许折叠与展开，默认优先显示事实和下一步。

## 9. 流程验收用例

| 用例 | 步骤 | 预期 |
|---|---|---|
| 首次创建 | Today → 创建 Matter → 写第一行动 | 3 分钟内完成，不需要理解全部模型 |
| 低能量日 | 选择“疲惫” → 查看 Today | 只保留必须守住和最小行动 |
| 无意识消耗 | Capture 记录刷视频 → 晚间复盘 | 可记录为事实，不自动判定为失败 |
| 暂停事项 | Matter → 暂停 → 次日打开 | 状态保持暂停，Today 不自动塞回 |
| 导出后改名 | 导出 Markdown → 改文件名 → 再导入 | 凭 Stable ID 恢复原 Matter |

## 10. 设计决策待定

- [ ] Today 是否默认显示 Stage，还是只显示自然语言解释？
- [ ] Capture 是否首版支持语音转文字？
- [ ] 用户是否可以在 Today 直接创建一个没有 Matter 的 Action？
- [ ] “很差”状态是否自动建议暂停非必要 Matter，还是只提供提醒？
