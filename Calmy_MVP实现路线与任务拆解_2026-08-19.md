# Calmy MVP 实现路线与任务拆解

> 将 MVP 设计转成工程、设计、AI、数据和 QA 可以并行执行的任务。  
> 版本：v0.1 · 日期：2026-08-19

关联文档：

- [MVP 产品范围与不做清单](Calmy_MVP产品范围与不做清单_2026-08-19.md)
- [领域模型与状态机](Calmy_领域模型与状态机_2026-08-19.md)
- [领域命令与 Repository 契约](Calmy_领域命令与Repository契约_2026-08-19.md)
- [核心用户流程与界面规格](Calmy_核心用户流程与界面规格_2026-08-19.md)
- [MVP 验证与测试计划](Calmy_MVP验证与测试计划_2026-08-19.md)

## 1. 交付目标

MVP 只交付一条可连续使用的个人闭环：

```text
Capture → Matter → Today → Action → Record → Review → 调整下一步
```

完成标准不是“页面都做出来”，而是用户可以在两周内真实使用这条闭环，并且：

- 数据不会因为刷新、离线、导出或错误操作丢失；
- 系统不把推断写成事实；
- 用户可以暂停、回退、放下和恢复；
- AI 和未来同步都不能绕过 Domain Layer。

## 2. 交付原则

- 先做领域规则，再做页面装饰。
- 先做本地事实源，再做云端同步。
- 先做可撤销建议，再做自动化。
- 先用合成数据和真实用户流程验证，再扩展关系和开放同步。
- 每个功能必须对应一个 P0 / P1 验收场景；没有验收场景的功能暂不进入 MVP。

## 3. 阶段路线

| 阶段 | 目标 | 主要产出 | 退出条件 |
|---|---|---|---|
| 0. 领域基线 | 固定对象、命令、事件和错误语义 | Domain types、Repository interface、fixtures | P0 领域单元测试通过 |
| 1. 本地闭环 | 不依赖网络完成 Capture → Review | IndexedDB、Today、Matter、Action、Record | 离线核心流程通过 |
| 2. 体验收敛 | 降低首次使用和日常操作成本 | UI、空状态、键盘、移动端 | 可用性任务达到目标 |
| 3. 可迁移 | 数据能带走、能回来 | Markdown Adapter、manifest、导入导出 | Stable ID 和关系恢复通过 |
| 4. AI 辅助 | 让 AI 做结构化建议而非代理 | AI Contract、证据链、确认流 | AI 越权测试全部通过 |
| 5. 试用发布 | 小范围真实验证 | telemetry、错误报告、回滚方案 | P0 全通过，无数据安全阻塞 |

## 4. 工作流拆解

### 4.1 Domain / Repository

| ID | 任务 | 依赖 | 完成定义 |
|---|---|---|---|
| D-01 | 定义 `Person / Matter / Cycle / Action / Record / Today` 类型 | 无 | 类型与最小字段和文档一致 |
| D-02 | 实现状态机和合法转换 | D-01 | 非法转换返回稳定错误，不产生部分写入 |
| D-03 | 实现 Capture 升级为 Matter 的判断器 | D-01 | 0–1 项不建议，≥2 项可建议，用户可拒绝 |
| D-04 | 实现 `Record` 与 `Observation / Insight / Trajectory` 分层 | D-01 | 每个解释可带 evidence、confidence、confirmation |
| D-05 | 定义 Domain Command 与事件 | D-01、D-02 | 所有写入均经过 Command |
| D-06 | 实现 IndexedDB Repository | D-05 | 可离线读写、刷新恢复、幂等提交 |
| D-07 | 实现 Mutation Log 与 Projection 重建 | D-06 | 删除并重建 Projection 后状态一致 |
| D-08 | 实现 `b_version` 迁移入口 | D-06 | 旧 fixture 可以迁移到当前版本 |

### 4.2 Today / Capture / Review

| ID | 任务 | 依赖 | 完成定义 |
|---|---|---|---|
| U-01 | Today 三问 | D-06 | 重要什么、为什么、什么可以不做可保存 |
| U-02 | 承载量与降载策略 | D-02、U-01 | 疲惫 / 很差时默认减少展示和候选 |
| U-03 | 全局 Capture | D-06 | 10 秒内保存未分类文本 |
| U-04 | Inbox / Triage | D-03 | 原文不丢；分类可跳过 |
| U-05 | Matter 详情 | D-01、D-02 | 显示当前 Cycle、主要矛盾、历史和今日连接 |
| U-06 | Action 执行卡片 | D-02 | 完成、跳过、取消、延期均可追踪 |
| U-07 | Evening Review | D-04 | 观 → 察 → 调 → 土库可保存或跳过 |
| U-08 | 空状态和错误恢复 | U-01–U-07 | 无数据、离线、冲突均有可行动提示 |

### 4.3 Open Format / 数据迁移

| ID | 任务 | 依赖 | 完成定义 |
|---|---|---|---|
| O-01 | Markdown Serializer | D-01 | 输出可读 YAML + Markdown |
| O-02 | Markdown Parser | O-01 | 可解析合法文件并验证类型和版本 |
| O-03 | Manifest | O-01 | 记录 path、hash、revision、last_synced_at |
| O-04 | 导出压缩包 / 文件夹 | O-01、O-03 | 普通文本编辑器可读 |
| O-05 | 重新导入 | O-02、D-06 | Stable ID 和 Relation 恢复 |
| O-06 | 错误与待处理列表 | O-02 | 坏文件不阻塞其他文件导入 |

### 4.4 AI

| ID | 任务 | 依赖 | 完成定义 |
|---|---|---|---|
| A-01 | 定义 AI 输出 schema | D-05 | observation / suggestion / draft / confirmed 可区分 |
| A-02 | Capture 结构化建议 | A-01、U-03 | 给出候选，不自动写入 |
| A-03 | Review 草稿 | A-01、U-07 | 引用来源 Record，用户可修改 |
| A-04 | 最小行动建议 | A-01、U-02 | 考虑承载量，不自动添加 Today |
| A-05 | Domain Command 确认流 | A-01、D-05 | 所有持久化变更有 command_id 和 confirmed_by |
| A-06 | AI 越权与 prompt injection 测试 | A-01、A-05 | P0 AI 对抗用例通过 |

### 4.5 QA / Release

| ID | 任务 | 依赖 | 完成定义 |
|---|---|---|---|
| Q-01 | Domain 单元测试 | D-01–D-08 | 状态机、升级条件、分层规则覆盖 |
| Q-02 | Repository 集成测试 | D-06、D-07 | 离线、幂等、迁移、重建通过 |
| Q-03 | P0 E2E | U-01–U-08 | 核心闭环可自动重复执行 |
| Q-04 | 导入导出测试 | O-01–O-06 | 改名、移动、冲突、坏文件通过 |
| Q-05 | 可用性测试 | U-01–U-08 | 5 项任务达到目标 |
| Q-06 | 无障碍 / 移动端检查 | U-01–U-08 | 无阻塞性问题 |
| Q-07 | Release smoke | 全部 | P0 通过、无数据丢失、可回滚 |

## 5. 推荐实施顺序

```text
D-01 → D-02 → D-05 → D-06 → U-03 → U-05 → U-01
                                  ↓
                         U-06 → U-07 → Q-03
                                  ↓
                         O-01 → O-02 → O-05
                                  ↓
                         A-01 → A-02 → A-05 → A-06
```

不要先做：

- 复杂五行图谱；
- Shared Space；
- 实时双向同步；
- AI 自动代理；
- 大量趋势图和装饰性 Dashboard。

## 6. Definition of Done

每个工程任务完成前必须满足：

- [ ] 有明确的用户或领域行为描述。
- [ ] 有至少一个成功路径和一个错误 / 边界路径。
- [ ] 不违反领域不变量和数据所有权原则。
- [ ] 有单元、集成或 E2E 测试中的对应位置。
- [ ] 错误可被用户理解，且不会静默丢数据。
- [ ] 离线、刷新或重复提交行为已经说明。
- [ ] 相关 Markdown 文档已同步更新。

## 7. 首批工程切片

建议第一个可运行切片只包含：

1. 创建一个 Matter。
2. 创建一个 Action。
3. 将 Action 加入 Today。
4. 完成或跳过 Action。
5. 写一条 Record。
6. 刷新页面后仍能看到完整历史。

这个切片不需要 AI、云端、五行图、People 或 Markdown 同步，但必须把 Domain Layer、Repository 和 Mutation Log 的边界搭好。

## 8. 发布阻塞条件

以下任一问题存在，直接停止发布：

- 任何 Record 或 Matter 在刷新、离线、导入时丢失；
- 同步或导入静默覆盖用户修改；
- AI 可以未经确认修改关键状态；
- 用户无法暂停、恢复或查看真实负向记录；
- 领域状态与 UI 显示不一致；
- 没有办法从当前对象追溯来源 Record 或变更事件。

## 9. 2026-08-19 Open Format 进度

第一阶段已落地：

- Markdown 文件 + YAML frontmatter；
- calmy_id、calmy_type、b_version、revision 稳定字段；
- _calmy/manifest.json 路径、版本、revision、hash 清单；
- Matter / Action / Reality Record / Daily 的导出与重新解析；
- 文件改名或移动仍按稳定 ID 识别；
- 重复 ID、坏 frontmatter、manifest hash 不一致均阻断导入；
- Matters 页面提供目录导出、目录导入和明确确认写入。

本轮继续完成：

- O-07：增量 revision / hash / manifest 比对，支持旧 manifest 路径下的文件移动识别；
- O-08：导入前显示新增 / 未变化数量，支持逐条选择保留本地、采用 Vault 或字段级手工合并；未选择的冲突不会写入。
- O-10：附件写入 assets/，manifest 记录 MIME、大小、hash 和引用关系；同路径内容变化或缺失引用会阻断导入，孤儿附件只提示不自动删除。
- O-09（第一阶段）：Obsidian Vault Adapter、幂等写入、变更监听和插件校验骨架。

仍按后续阶段处理：

- O-09（后续增强）：Calmy companion bridge、导入预览命令和确认后的双向写回；
- O-10（后续增强）：孤儿附件清理预览与显式删除。
