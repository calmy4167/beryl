# Calmy MVP 产品范围与不做清单

> 从《Calmy / Personal OS 完整产品设计主档案》拆出的执行文档。  
> 版本：v0.1 · 日期：2026-08-19 · 状态：讨论稿

## 0. 这份文档解决什么问题

主档案已经回答了 Calmy 想成为什么；本文件进一步回答：第一版具体做什么、明确不做什么、如何验收。

## 1. MVP 北极星

第一版只证明一条闭环：

> 用户能把一个现实问题放进 Matter，连接到今天最小必要行动，记录真实发生，并在晚上得到下一步调整。

成功不以任务数量、连续打卡或完成率为核心，而以以下变化为核心：

- 用户更快知道今天真正重要的 1–3 件事。
- 用户能理解这些行动为什么重要，以及它们连接哪个 Matter / Cycle。
- 用户可以记录完成、未完成、回退、逃避、娱乐和损耗。
- 用户能暂停、降载、恢复或放弃，而不会被系统默认为失败。

### 1.1 第一版核心任务与非核心能力

第一版只围绕一条主链路投入设计和工程资源：

```text
现实 Capture → Matter → 今日最小行动 → 真实 Record → 下一步调整
```

以下能力属于非核心层，只保留接口和最小展示，不得反过来改变 MVP 的主流程：

- 五行：作为内部 Stage 语义，默认不要求用户学习。
- AI：作为可撤销的结构化建议，不作为自动代理。
- Person / Relationship：先作为上下文关联，不做完整共享空间。
- Library / Obsidian：先保证可读、可导出、可重新导入。

判断新功能是否进入 MVP 时，先问：**它是否直接帮助用户完成“今天的最小行动并理解反馈”？** 如果不能，进入后续版本。

## 2. 目标用户情境

| 情境 | MVP 应该帮助用户完成什么 |
|---|---|
| 长期学习 / 职业转型 | 把长期 Matter 连接到当前 Cycle 和今天的最小行动 |
| 状态波动 / 精力不足 | 依据身体、心理和承载量降载，而不是继续加压 |
| 计划很多但行动少 | 低摩擦 Capture；Today 只保留核心行动 |
| 反复开始又中断 | 记录暂停、回退、恢复，并保留真实历史 |

## 3. MVP 范围

### 3.1 必须包含

| 能力 | 第一版要求 | 明确不扩展 |
|---|---|---|
| Today | 三问、承载状态、1–3 个核心行动、可放下事项 | KPI 仪表盘、连续打卡排名 |
| Capture | 快速记录文本，进入 Inbox | 一开始就要求用户判断类型和层级 |
| Matters | 创建、查看、暂停、归档 Matter | 无限自定义对象和模块 |
| Cycle / Stage | 主题、当前阶段、暂停 / 完成 / 回退；五行轻量显示 | 递归 Cycle 的复杂树形编辑器 |
| Action | 行动描述、状态、所属 Matter / Cycle、完成与跳过 | 完整项目管理、依赖图、复杂排期 |
| Record | 事实、状态、时间流向、反馈、负向变化、复盘 | 自动判断用户是否“浪费” |
| Review | 观 → 察 → 调 + Seed | 年度复杂趋势分析 |
| Portability | Markdown + YAML 基础导出与导入 | 实时双向同步、Obsidian 插件 |

### 3.2 最小对象集合

| 对象 | 最小字段 | 生命周期 |
|---|---|---|
| `Person` | `calmy_id`、`name`、`is_self` | `active / archived` |
| `Matter` | `calmy_id`、`title`、`why`、`status`、`current_cycle_id`、`trajectory` | `active / paused / archived` |
| `Cycle` | `calmy_id`、`matter_id`、`theme`、`current_stage`、`status`、`started_at` | `active / paused / completed` |
| `Action` | `calmy_id`、`title`、`matter_id`、`cycle_id`、`date`、`status` | `planned / done / skipped / cancelled` |
| `Record` | `calmy_id`、`type`、`occurred_at`、`body`、`matter_id`、`source` | 追加为主；更正保留历史 |
| `Today` | `date`、`load`、`focus_action_ids`、`must_protect`、`let_go` | 每天一条，可补写 |

### 3.3 Capture 升级为 Matter 的条件

Capture 不应一开始就被迫升级成 Matter。满足以下条件中的至少两项时，系统才建议升级：

- 会持续一段时间；
- 包含多个行动；
- 需要判断、取舍或资源安排；
- 会消耗时间、精力、金钱或关系资源；
- 值得复盘并形成经验。

否则优先保留为 `Record` 或 `Action`，减少用户维护系统的成本。

## 4. 核心闭环

1. **Morning Opening**：回答“今天真正重要什么、为什么、今天什么可以不做”。
2. **Capture**：任何新想法或现实事件先进入 Inbox，不打断记录。
3. **Triage**：用户或 AI 建议把 Capture 连接到 Person、Matter、Action 或 Record；用户确认后生效。
4. **Action**：执行最小必要行动，允许跳过、暂停、降载或改写。
5. **Record**：记录事实、时间、状态、反馈、娱乐、逃避、损耗与成果。
6. **Evening Review**：观事实，察条件，调明天，并决定是否产生下一轮 Seed。

## 5. 首版页面

### Today

- 方向：当前最重要的长期 Matter 和 Cycle Theme。
- 当下：身体、心理、精力和今日承载量。
- 行动：1–3 个核心 Action，分为“必须守住”和“有余力再做”。
- 减法：今天什么可以不做。
- Capture：固定、随时可用。

### Matters

- 当前 Matter、状态、trajectory、主要矛盾。
- 当前 Cycle、Stage、最近 Action、最近 Record。
- 暂停、恢复、归档、创建下一轮。

### Search / Library

- 搜索历史 Record、Matter、Action、Insight 和 Seed。
- 第一版 Library 可以只是结构化历史与导出入口，不做完整知识库。

## 6. 不做清单

- 不做完整财务、习惯、博客、番茄钟和独立知识库系统。
- 不做默认强制的五行流程；简单动作不展开五行。
- 不做无限递归 UI；深层关系使用 breadcrumb 和关联卡片。
- 不做 AI 自动创建、删除、归档或修改关键历史。
- 不做多人共享、情侣 / 家庭协作的完整交互；只预留数据接口。
- 不做实时双向同步；先验证可导出、可读、可重新导入。
- 不做排行榜、连续天数和“完成越多越好”的激励机制。

## 7. 状态与边界

### Matter

```text
draft → active → paused → active
active → archived
paused → archived
archived → restored → paused
```

### Action

```text
planned → in_progress → done
planned → skipped
planned / in_progress → cancelled
```

### 设计约束

- `paused` 是合法状态，不是失败。
- `archived` 不是删除，历史仍可搜索、导出和恢复。
- 完成 Action 只记录事实，不自动生成“成功”结论。
- `Outcome` 与 `Practice` 分开：结果是信息，实践是可控行动。

## 8. 发布验收标准

- [ ] 用户在 3 分钟内创建一个 Matter，并写出第一条最小行动。
- [ ] Today 能显示方向、当前承载、核心行动和可放下事项。
- [ ] 可以记录未完成、回退、逃避、主动娱乐和损耗。
- [ ] 暂停 Matter 后，系统不会第二天自动塞回 Today。
- [ ] 刷新、离线、重新打开后核心数据仍然存在。
- [ ] 导出的 Markdown 在普通文本编辑器中可读，并能再次导入基本对象。
- [ ] 构造同步冲突时，系统不会静默覆盖用户修改。

## 9. 版本顺序

| 阶段 | 重点 | 进入条件 |
|---|---|---|
| MVP | Today、Capture、Matter、Cycle、Action、Record、Review、基础导入导出 | 核心闭环可连续使用两周 |
| v1.1 | AI 结构化建议、搜索、Trajectory、更多开放格式字段 | 用户愿意持续记录，且不被 AI 打扰 |
| v1.2 | People / Relationship Context、共享空间只读视图 | 个人模型稳定，权限边界明确 |
| v2 | 增量同步、冲突处理、Obsidian 双向桥 | 已有真实迁移需求与可验证协议 |

## 10. 关系系统边界

MVP 只支持“我自己的生活系统”。`Person` 可以作为 Matter 的关联上下文，但不进入多人协作流程。

首版明确不支持：

- 共享空间中的多人编辑；
- 情侣 / 家庭成员的细粒度权限；
- 自动读取或推断他人的隐私信息；
- 代表他人创建 Action、修改状态或写入关系记录；
- 把关系质量转化成分数、排名或自动结论。

后续进入 Relationship / Shared Space 前，必须先完成：

- 明确 actor、owner、editor、viewer 的权限模型；
- 明确私人 Record 与共享 Record 的边界；
- 所有跨人读取都需要可见的授权范围；
- 冲突记录必须显示“谁在什么时候修改了什么”；
- 用户可以撤回共享、导出和删除自己的内容。

## 11. 产品指标

指标用于判断产品是否让用户更清楚，而不是制造新的自我考核：

- 从 Capture 到确定下一步的时间；
- 用户在 1 分钟内确定 Today 核心行动的比例；
- Action 与 Matter 的有效连接比例；
- 用户主动删除、延期或暂停不必要行动的比例；
- 用户是否持续记录真实情况，而不只是完成情况；
- AI 建议被接受、修改、拒绝的比例；
- 用户自评“更清楚 / 更能选择 / 更能适应现实”的变化。

## 12. 待定决策

- [ ] MVP 是否严格只支持单人？
- [ ] 负向记录使用自由输入，还是少量可选标签？
- [ ] 五行 Stage 在 Today 默认显示，还是只在 Matter 详情显示？
- [ ] 第一版导入导出是否同时处理附件？
