# Calmy Personal OS 当前交接入口

> 更新时间：2026-08-29
> 性质：工程续接索引，不再重复保存已完成任务明细。

## 下一轮先读

1. [`docs/README.md`](../README.md)：文档权威层级。
2. [`docs/product/CALMY_UNIFIED_PRODUCT_DESIGN_2026-08-29.md`](../product/CALMY_UNIFIED_PRODUCT_DESIGN_2026-08-29.md)：统一产品、体验、AI、Flow 和阶段设计。
3. [`docs/PROJECT_STRUCTURE.md`](../PROJECT_STRUCTURE.md)：当前生产入口、代码目录和文件放置规则。
4. [`docs/product/OPEN_WORK.md`](../product/OPEN_WORK.md)：唯一活跃未完成工作。
5. [`IMPLEMENTATION_BASELINE_2026-08-19.md`](../implementation/IMPLEMENTATION_BASELINE_2026-08-19.md)：实现证据快照。

原始 DOCX 只读；不要从已删除的旧任务拆解或 Beryl 交接文件恢复任务。

## 当前实现事实

- 生产入口是 `index.html → src/react/main.tsx → src/react/App.tsx`。
- React 已承接核心 Today、Capture、Matters、Review、Cycle、我的和扩展模块路由。
- 桌面端具备左主栏和默认收起的独立右栏；窄桌面和移动端使用 More 抽屉。
- 现有领域、应用用例、异步 Repository、IndexedDB durable 边界、导入导出、同步兼容层和测试证据以实现总档案为准。
- Finance、Pomo、Inbox 的直接同步写入已收口到异步 Repository；Finance/Inbox 跨仓储流程已提取为 Application Use Case 并完成幂等测试，不应恢复旧版同步写入作为新事实源。
- 实体迁移回滚必须使用 `rollbackMigrationDurable`；旧的同步 `rollbackMigration` 仅保留给自定义 Storage/兼容测试，不作为生产管理页入口。
- `applyEntityRecords` 现在是异步入口；新增同步调用必须 `await`，以保证远端实体应用完成 durable flush 后再推进同步流程。
- 实体同步与键级主同步的 pull cursor 现在优先从 IndexedDB `meta` 恢复，并在推进前等待事务确认；IndexedDB 不可用时才回退到旧 localStorage 游标。
- `store.set` 的业务集合值、键级变更日志和实体级变更日志现在通过同一 IndexedDB 事务提交；空集合首次写入也会生成实体 create 日志。
- pending write 重放会保留该次写入的实体日志上下文和稳定 ID，崩溃恢复不会只恢复业务值而漏掉实体日志。
- 首次实体同步在 ready 之前从零拉取；本地快照推送后再次完整拉取确认，远端应用成功后才推进 ready/cursor。
- 立即同步的冲突/远端更新提前返回分支，以及启动自动恢复的云端分支，均会继续执行实体同步，避免只完成键级同步就结束流程。
- 共享协作异步写入口支持末尾的调用方命令 ID；需要跨网络重试的创建、更新、状态变更和 Record 修订应复用同一个命令 ID，避免重复实体与审计。
- React Review 使用 `listActionRecordDocumentsAsync` 读取 Action/Record 证据；新增 React 跨域查询应优先复用异步查询入口，不要重新调用同步 `listRealityDocuments`。
- 全量 React Reality 查询使用 `listRealityDocumentsAsync`；新增 React 页面应复用该入口，只有 Vue 兼容层和未迁移旧模块继续使用同步查询。
- Vue、Vue Router、Element Plus 和旧 Vue 页面仍是迁移兼容层，不是当前生产主入口。
- 2026-08-29 入口审计已由静态回归和浏览器 smoke 确认：`index.html → src/react/main.tsx → src/react/App.tsx` 是唯一生产启动链，React Admin 与 Vue `admin/advanced` 均可实际加载且返回后旧桥接卸载；未被生产路由引用的 `LegacyVueHost` 已完成第一批清理，旧 `/app/cases`、`/app/cases/:id`、`/app/module/chars`、`/app/module/moments` 和未知 `/app/module/:id` 兼容重定向也已回归，Vue 只在 `LegacyAdminHost` 和同步基础设施中保留，OW-06 仍需完整旧路由回归后再分批退出。
- OW-07 三刀已落地：扩展页和兼容桥的 React lazy 注册集中在 `src/react/lazy-pages.ts`，工作台壳层/搜索弹层在 `src/react/AppShell.tsx`，共享 Button、页面头部和焦点陷阱在 `src/react/ui.tsx`，路由树/兼容重定向/Suspense 边界在 `src/react/routes.tsx`；App 现只负责启动、守卫组件和页面节点装配，URL 与兼容行为保持不变，后续继续拆页面状态。

### 2026-08-29 档案校准

- 当前 React 主路径的 Reality 查询已统一到异步入口；`src/react/bootstrap.ts` 中保留的同步模块统计 reader 仅服务旧统计兼容，不是 React 页面数据查询入口。
- 实体同步应用远端记录时会先比较本地变更日志版本，并在真正写入后等待 durable flush；共享协作调用方命令 ID 会贯穿实体写入与审计，重复提交不新增事实。
- 当前自动化基线为 58 个 Vitest 文件、292 个测试通过；Node 15/15、同步协议 22/22、IndexedDB 浏览器运行时、类型检查、构建、PWA、性能和 UI smoke 均通过。实体值/实体日志原子提交、首次实体同步完整合并确认、实体同步版本与批量顺序、键级同 key 最终版本/同毫秒设备决胜/墓碑覆盖、失败时不推进游标、实体/键级 pull cursor 与 push cursor durable 确认、键级同步业务白名单隔离、键级同步/实体同步编排边界、React 页面同步 Reality 隔离、React/Vue 生产入口隔离、Capture 决策命令幂等以及 React 工作台键盘边界回归已补齐；UI smoke 新增 320px Today/Capture/More 与 CDP 200% page-scale 布局回归。CDP 的视觉视口与 DOM 布局坐标存在模拟边界，OW-04 的真实设备、真实浏览器缩放、键盘连续操作、读屏、大字号和异常恢复验收仍待人工执行。

本轮 OW-03 兼容复核还修正了旧 Worker 全量写入失败被吞掉的问题：旧协议写入现在返回明确结果，失败进入统一同步错误状态，成功后才结束 `syncing`。

## 当前工作纪律

- 新任务只写入 `docs/product/OPEN_WORK.md`，完成后从活跃清单移除并在实现档案留下证据。
- OW-03 的兼容/历史/高级同步复核已收口；下一步先完成 OW-04 的真实设备和辅助技术验收，当前键级立即同步/自动恢复已接入实体同步，旧模型真实形态迁移演练已完成，不再作为前置任务，再扩展 Flow、AI、Bridge 或共享空间。旧 Vue 同步查询仍由 OW-06 管理。
- OW-04 代码侧已补齐 React 登录错误的即时播报和导入文件控件 accessible name；剩余仍是无法由本地自动化替代的真实读屏、系统大字号和设备缩放验收。
- 不修改原始 DOCX；不使用破坏性回滚覆盖用户工作区改动。
- 每个实现切片完成后运行类型检查、全量测试、浏览器 smoke、PWA 和生产构建。
