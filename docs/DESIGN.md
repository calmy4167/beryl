# Beryl v2 · 设计文档

> 个人管理体系（Vue 3 + Element Plus + TypeScript，Cloudflare Pages 静态托管）

---

## 一、设计原则

设计原则是 Beryl 一切界面决策的上位准则。所有页面、组件、动效、配色改动都必须能对到某条原则上；对不上原则的改动不做。

### 1. 内容优先，界面退后
背景与容器保持中性，让数据与内容自己说话。界面元素（边框、底色、装饰）只承担分层职责，不承担注意力职责。
- 落地：白底/深底 + 1px 细边框卡片；无重阴影、无玻璃拟态装饰、无渐变炫技。

### 2. 清晰的视觉层级
全站统一三级字号（标题 / 正文 / 辅助小字）与三级文字色（主 `--c-text` / 次 `--c-text-2` / 弱 `--c-text-3`）。任何一屏内，用户应在 3 秒内说出"先看什么、再看什么"。
- 落地：hero 2.25–2.5rem → 正文 14px → 辅助 10–12px；字重 700/600/400。

### 3. 单强调色 + 中性色
全局只有一个交互强调色（天蓝，场景模式可替换为场景色），只用于：选中态、主按钮、链接、进行中状态。其余一切用中性灰阶。
- 语义状态色（绿/红/琥珀）只表示结果（成功/错误/警告），不参与装饰。

### 4. 一致的节奏（4/8px 网格）
间距、圆角、边框遵循同一节奏：大卡 16px、小卡 10px、卡片间距 10–14px、区块间距 16/32px。同类元素必须长得一样。

### 5. 克制的动效
动效只做反馈，不做表演。时长 150–200ms，缓动 ease；hover 上浮、箭头位移、折叠展开、主题切换、加载渐入是允许的；弹跳、循环、放大镜式效果禁止。

### 6. 状态可见
选中 / 悬停 / 禁用 / 加载 / 错误 / 拖拽中，每个状态都有明确视觉表达。不允许"点了没反应"的控件。
- 落地：导航激活 = 天蓝浅底 + 左侧 2px 强调条；按钮 loading；卡片 hover 描边；拖拽插入线 = 天蓝粗边；键盘焦点可见。

### 7. 可访问性
- 正文对比度 ≥ 4.5:1（WCAG AA）；辅助文字 ≥ 3:1。
- 全部交互元素可键盘操作且有可见焦点（focus-visible 天蓝环）。
- 语义化标签（button / nav / label），不滥用 div 承担交互。

### 8. 响应式与自适应
窄屏（<768px）：抽屉导航；中屏：侧边栏可折叠为图标栏；宽屏：内容容器上限 960px，网格 auto-fill 自动增列。内容永远不横向溢出。

### 9. 温柔的随机（随机性 / 未知性，但不包含恐惧与得失）
系统应当有"活着的感觉"，但随机性只作用于**呈现层**，绝不触碰**数据层**。这是本项目随机性的总纲：

> **定义**：随机的未知性 = 每次进入/刷新/操作时，呈现上有可预期范围内的变化，给用户"惊喜感"而非"失控感"。
>
> **允许的随机**（无恐惧、无得失）：
> - 内容类：随机名言、随机引言、随机卡片组合与排列顺序、大卡标题语料池随机
> - 呈现类：卡片间距微调、装饰色从预设色板内随机取色
> - 文案类：欢迎语、空状态提示语在预设语料池中随机
> - 时机类：加载完成后的短暂渐入（不做循环动画）
>
> **禁止的随机**（含恐惧/得失）：
> - 随机删除、修改、移动用户数据
> - 随机改变功能行为（按钮做了 A 却触发 B）
> - 随机改变已保存的设置与主题
> - 无限循环 / 不可关闭的自动变化
> - 会让用户担心"是不是出 bug 了"的视觉突变
>
> **检验标准**：任何一个随机元素被问"它为什么会变？"，答案只能是"为了让界面有生气"，而不是"系统失控了"。

### 10. 直接操纵与可逆性
用户可以直接拖拽卡片调整顺序、拖动手柄调整大小，操作即时生效且视觉反馈明确（插入线、虚线框）。布局类操作**只影响呈现**、可持久化、可随时重排，永不触碰数据内容。
- 落地：大卡拖拽排序（天蓝插入线）、大卡 ⤡ 手柄调大小（1 列 ⇄ 整行）、小卡组内拖拽排序；布局存 `b_wall_layout`。

### 11. 数据本地优先
所有用户数据先落本地（localStorage），网络只做可选同步与内容增强（如名言拉取）。断网时系统完整可用（离线名句池兜底，随机抽取不重复）。

---

## 二、色彩体系

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `--c-bg` | `#ffffff` | `#0d1117` | 页面背景 |
| `--c-bg-soft` | `#f6f8fa` | `#161b22` | 次级表面（侧边栏/小卡底） |
| `--c-bg-2` | `#eef1f4` | `#21262d` | 更深层次（代码块/标签） |
| `--c-text` | `#1f2328` | `#f0f6fc` | 主文字 |
| `--c-text-2` | `#57606a` | `#8b949e` | 次要文字 |
| `--c-text-3` | `#8c959f` | `#6e7681` | 弱文字/占位 |
| `--c-border` | `#d0d7de` | `#30363d` | 主边框 |
| `--c-border-soft` | `#e6e9ec` | `#21262d` | 卡片边框 |
| `--scene` | `#0ea5e9` | `#38bdf8` | 强调色（场景可覆盖） |
| `--c-success` | `#1a7f37` | `#3fb950` | 成功 |
| `--c-danger` | `#cf222e` | `#f85149` | 错误 |
| `--c-warn` | `#9a6700` | `#d29922` | 警告 |

主题切换：默认浅色，侧边栏 🌙☀️ 切换，`b_theme` 持久化。

---

## 三、布局结构

```
┌──────────┬──────────────────────────────┐
│ 侧边栏    │  内容区（max 960px）            │
│ 232px    │  ┌────────────────────────┐  │
│ ▸品牌     │  │ hero（徽标+标题+随机引言） │  │
│ ▸首页     │  ├────────────────────────┤  │
│ ▸分组导航  │  │ 合并输入框（随想⇄待办）    │  │
│  （激活=   │  ├────────────────────────┤  │
│  天蓝高亮）│  │ 卡片墙：大卡嵌小卡          │  │
│ ▸折叠 «   │  │ ┌────────────────────┐ │  │
│ ▸后台/主题 │  │ │🖋 大卡（拖排序/调大小）│ │  │
└──────────┘  │ │ ┌────┐ ┌────┐ ┌────┐│ │  │
  折叠态 64px  │ │ │小卡│ │小卡│ │小卡││ │  │
  移动端 ☰ 抽屉 │ │ └────┘ └────┘ └────┘│ │  │
              │ └────────────────────┘ │  │
              └────────────────────────┘  │
              （两列，大卡可跨整行）          │
              └──────────────────────────┘
```

---

## 四、卡片墙架构（首页核心）

### 层级
```
QuoteWall（容器：构建大卡组 + 布局持久化 + 换一批）
  └─ GroupCard（大卡片：标题栏 + 拖拽排序 + ⤡ 调大小 + 内部小卡网格）
       └─ QuoteCard（小卡分发器：类型注册表 RENDERERS，未知 kind 兜底）
            ├─ cards/PersonQuoteCard.vue   人物+名句小卡（头像块嵌套）
            ├─ cards/QuoteOnlyCard.vue     单独名句小卡
            └─ cards/PersonCard.vue        人物小卡
```

### 数据流（`src/core/quotes.ts`）
```
buildGroups()
  ├─ 网络源（多源备用）：一言 hitokoto → 今日诗词 → 离线池（18 条随机不重复）
  │    有作者 → person-quote；无作者 → quote
  ├─ 本地源：人物库（b_chars）→ person 小卡
  └─ 组装三张大卡：
       🖋 名句组（标题随机：灵感碎片/思考片刻/远方与诗…）
       💬 人物·语录组（网络带作者名句 + 本地人物配离线名句）
       👥 人物库组
```

### 布局持久化（`b_wall_layout`）
`{ order: string[], spans: Record<id, 1|2> }` —— 大卡顺序 + 跨列数，刷新/换一批后尽量恢复（按 kind 匹配）。

### 解耦约定
- 新增小卡类型：`quotes.ts` 加 kind → `cards/` 加渲染器 → `QuoteCard.vue` 注册表加一行
- 新增大卡类型：`quotes.ts` 的 buildGroups 加一组即可
- 小卡不感知大卡结构，大卡不感知小卡内部实现

---

## 五、交互规范

| 交互 | 行为 | 反馈 |
|---|---|---|
| 拖大卡标题栏 | 排序 | 天蓝插入线（上/下半指示），松手插入 |
| 拖大卡 ⤡ 手柄 | 调大小 | 1 列 ⇄ 整行，拖拽中虚线框 |
| 拖小卡 | 组内排序 | 目标位半透明 |
| 换一批 ↻ | 重新随机组合 | loading 禁用态 |
| 主题切换 🌙☀️ | 深浅色 | 即时切换，持久化 |
| 侧边栏 « | 折叠/展开 | 200ms 宽度过渡 |

---

## 六、页面清单

| 路由 | 页面 | 要点 |
|---|---|---|
| `/login` | 登录 | 品牌 Logo 块 + 卡片表单 + 锁定倒计时 |
| `/pass` | 设置/修改密码 | 首次登录强制改密 |
| `/scene` | 场景选择 | 4 场景卡片网格，选中场景色描边 |
| `/app/home` | 首页 | hero + 合并输入框 + 大卡片墙（随机+拖拽） |
| `/app/module/:id` | 模块页 | 9 个功能模块，文档化头部 |
| `/app/admin` | 后台 | 数据/场景/同步管理 |

---

## 七、随机性落地现状

- [x] 名言网络拉取（多源备用：一言 / 今日诗词）
- [x] 离线名句池随机抽取不重复（18 条）
- [x] hero 随机引言（10 句语料池）
- [x] 空状态随机提示语（4 句）
- [x] 大卡标题随机（6 个主题词）
- [x] 卡片组合与顺序随机（洗牌）
- [x] 加载骨架屏（静态灰块模拟大卡嵌小卡）+ 大卡渐入动画（一次性，stagger 70ms）
- [x] 布局按 kind 恢复（换一批后大卡顺序与大小保持）

## 八、已知限制与下一步

- 移动端：HTML5 拖拽在触屏不可用（大卡排序/调大小暂限桌面；后续可引入 pointer 版拖拽或"编辑模式"按钮）
- 名言 API 内容质量不可控（后续可加长度/重复过滤与本地去重）
- 场景页/模块页尚未接入卡片墙体系（后续可让模块页复用 QuoteCard 注册表）

---

## 九、v2 路线图进度（对应 v1 文档 §14）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 工程化重构（Vite+Vue3+TS 功能平移） | ✅ |
| 2 | 数据层 IndexedDB：kv 持久镜像 + changes 变更日志 + 启动迁移/自动恢复 + b_version 迁移机制 | ✅ |
| 3 | 同步引擎：Cloudflare 增量协议（pull/push 游标）+ LWW（ts, deviceId）+ 变更日志推送 + 旧 Worker 回退 | ✅ |
| 4 | Worker 升级 D1：records/auth 表、LWW upsert、增量 API、旧 /api/data 兼容、KV 数据自动迁移 | ✅ 代码就绪，需部署（创建 D1 + 绑定 BERYL_D1） |
| 5 | PWA（manifest + sw.js + 图标）+ AES-GCM 加密（PBKDF2 派生，云端密文） | ✅ |

**部署要求（Cloudflare 控制台）**：创建 D1 数据库（如 beryl-d1）→ Pages 项目绑定 D1（变量名 `BERYL_D1`）→ 重新部署。旧 KV 绑定（`BERYL_KV`）保留一次部署可自动迁移旧数据，迁移完成后可解除。
